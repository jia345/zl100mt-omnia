{
    "definitions": {
        "lower": {
            "type": "string",
            "pattern": "^[a-z]+$"
        }
    },
    "oneOf": [
        {
            "properties": {
                "module": {"enum": ["redefinition2"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]},
                "data": {"$ref": "#/definitions/lower"}
            },
            "additionalProperties": false
        }
    ]
}
