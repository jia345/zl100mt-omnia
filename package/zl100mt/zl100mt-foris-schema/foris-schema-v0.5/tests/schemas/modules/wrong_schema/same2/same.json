{
    "oneOf": [
        {
            "properties": {
                "module": {"enum": ["same"]},
                "kind": {"enum": ["reply"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        }
    ]
}
