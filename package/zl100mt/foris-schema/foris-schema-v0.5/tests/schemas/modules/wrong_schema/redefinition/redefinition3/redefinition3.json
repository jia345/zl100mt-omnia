{
    "definitions": {
        "message": {
            "type": "string",
            "pattern": "^[a-z]+$"
        }
    },
    "oneOf": [
        {
            "properties": {
                "module": {"enum": ["redefinition3"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        }
    ]
}
