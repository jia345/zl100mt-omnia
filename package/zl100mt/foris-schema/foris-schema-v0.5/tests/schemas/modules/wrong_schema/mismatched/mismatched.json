{
    "oneOf": [
        {
            "properties": {
                "module": {"enum": ["other"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        }
    ]
}
