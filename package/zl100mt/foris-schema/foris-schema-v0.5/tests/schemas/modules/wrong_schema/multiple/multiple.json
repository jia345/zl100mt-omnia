{
    "oneOf": [
        {
            "properties": {
                "module": {"enum": ["multiple"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        },
        {
            "properties": {
                "module": {"enum": ["multiple"]},
                "kind": {"enum": ["request"]},
                "action": {"enum": ["get"]}
            },
            "additionalProperties": false
        }
    ]
}
